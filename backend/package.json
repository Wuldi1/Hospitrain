{
  "name": "emergency-drill-manager-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "clean:artifact": "rm -rf .artifact",
    "build:artifact": "npm run clean:artifact && mkdir -p .artifact && tar --exclude='.artifact' --exclude='node_modules' --exclude='.env' --exclude='data/drills.json' -cf - . | tar -xf - -C .artifact"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "uuid": "^9.0.1"
  }
}
