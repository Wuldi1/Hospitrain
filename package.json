{
  "scripts": {
    "build:backend:artifact": "npm --prefix backend run build:artifact",
    "build:frontend:artifact": "npm --prefix frontend run build:artifact",
    "build:artifacts": "npm run build:backend:artifact && npm run build:frontend:artifact"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "nodemon": "^3.1.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.10.1"
  }
}
